export default function getBaoMiddleware(getResponse, handleErrors) {
    return async (ctx) => {
        const res = await getResponse(ctx.req);
        switch (res.status) {
            case 403:
            case 404:
                return handleErrors ? ctx.sendRaw(res).forceSend() : ctx;
            default:
                return ctx.sendRaw(res).forceSend();
        }
    };
}
//# sourceMappingURL=bao.js.map